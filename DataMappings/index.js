var account = require('./accountMapping');
var contact = require('./contactMapping');
var engagement = require('./engagementMapping');
var opportunity = require('./opportunityMapping');
var product = require('./productMapping');

module.exports = {
    account,
    contact,
    engagement,
    opportunity,
    product
}